class AppConfig {
  // Coordenadas do local de trabalho (exemplo). Substituir em produção.
  static const double officeLat = -23.550520;
  static const double officeLng = -46.633308;
  static const double maxDistanceMeters = 100.0;
}
